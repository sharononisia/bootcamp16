name: first workflow
on: push
jobs: 
   my-job:
     runs-on: ubuntu-latest
     steps:
       - name: first-step
         run: echo "Hello, World!"
       - name: Read-File
         run: cat hello.txt